Structure:
DB.colllection("lion-bot")

Each doc in collection:
{
    _id:{} //Used by DB and can be ignored
    "text":"", //If no text include formateResponse will not add a text field
    "image_url":"" 
}